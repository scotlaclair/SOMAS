# SOMAS Python Dependencies

# File locking for concurrent state file access
filelock>=3.12.0

# YAML parsing (used by workflows and agents)
pyyaml

# API documentation generation
pdoc>=14.0.0

# Optional dependencies for direct LLM API integration
# These enable autonomous agent execution without GitHub Copilot dependency
openai>=1.0.0
anthropic>=0.18.0
